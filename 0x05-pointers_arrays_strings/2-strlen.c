#include "main.h"
#include <string.h>

/**
 * _strlen - prints the length of a string
 * @s: size of string
 * Return: size of string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
