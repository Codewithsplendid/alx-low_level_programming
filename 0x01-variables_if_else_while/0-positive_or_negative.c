#include <stdlib.h>
#include <time.h>
/* main - Entry point
 * if =< -1 print (n "is negetive")
 * else if => 1 print (n "is positive")
 * else print (n "is zero")
 */
 int main(void)
{
	int n;
	srand(time(0));
	n = rand() -RAND_MAX / 2;
	if (n =< -1 )
	return (0);
}
