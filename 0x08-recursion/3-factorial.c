#include "main.h"

/**
 * factorial - returns factorail of a number
 * @n: value
 * Return: factorial
 */
int factorial(int n)
{
	if (n < 0)
		return (-1);
	else if (n == 0)
		return (1);
	else
		return (n * factorial(n - 1));
}
